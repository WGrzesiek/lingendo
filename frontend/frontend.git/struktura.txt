my-app/
├── src/                     # Wydzielony katalog na kod źródłowy (zalecane dla większych projektów):contentReference[oaicite:2]{index=2}:contentReference[oaicite:3]{index=3}
│   ├── app/                 # App Router Next.js (pliki routingowe i strony)
│   │   ├── layout.tsx       # Główny layout aplikacji (np. nawigacja, stopka)
│   │   ├── page.tsx         # Strona główna
│   │   ├── (auth)/...       # Grupa routingu (np. podstrony logowania):contentReference[oaicite:4]{index=4}
│   │   └── (learning)/...   # Grupa routingu dla modułu nauki słówek
│   ├── components/          # Globalne, wielokrotnie używane komponenty UI
│   │   ├── common/          # Komponenty wspólne (przyciski, pola, itp.)
│   │   ├── ui/              # Prymitywy UI (np. komponenty z shadcn/ui)
│   │   └── icons/           # Ikony jako komponenty
│   ├── features/            # Moduły funkcyjne (feature modules):contentReference[oaicite:5]{index=5}
│   │   ├── auth/            # Moduł uwierzytelniania
│   │   │   ├── components/  # Komponenty UI specyficzne dla autoryzacji
│   │   │   ├── hooks/       # Hooki biznesowe dla autoryzacji
│   │   │   ├── services/    # Funkcje do komunikacji z API (np. login, rejestracja)
│   │   │   └── types/       # Definicje typów (np. User, Token)
│   │   ├── learning/        # Moduł nauki słówek (flashcards, sesje nauki)
│   │   │   ├── components/  # Komponenty UI sesji nauki (np. WordCard)
│   │   │   ├── hooks/       # Hooki zarządzające stanem sesji (np. useLearningSession)
│   │   │   ├── services/    # Serwisy API (pobieranie słów, wysyłanie wyników)
│   │   │   └── types/       # Typy danych dla sesji nauki (np. Word, Progress)
│   │   └── shared/          # Kod współdzielony między modułami
│   │       └── components/  # Wspólne komponenty używane w różnych modułach:contentReference[oaicite:6]{index=6}
│   ├── hooks/               # Globalne hooki (nieprzypisane do jednej funkcjonalności)
│   │   ├── useDebounce.ts   # Przykład: uniwersalny hook debouncingu
│   │   └── useLocalStorage.ts
│   ├── lib/                 # Kod pomocniczy (biblioteki)
│   │   ├── api/             # Klienci API i konfiguracja (np. instancja Axios):contentReference[oaicite:7]{index=7}
│   │   │   └── axios.ts     # Konfiguracja globalna Axios (base URL, interceptory)
│   │   └── utils/           # Inne utils (np. funkcje formatowania)
│   ├── providers/           # Dostawcy kontekstu/aplikacji
│   │   ├── ThemeProvider.tsx
│   │   ├── QueryProvider.tsx # Provider konfigurujący React Query (QueryClient):contentReference[oaicite:8]{index=8}
│   │   └── AuthProvider.tsx  # Provider autoryzacji (np. Kontext użytkownika)
│   ├── stores/              # Globalny store stanów aplikacji (np. Zustand)
│   │   └── useStore.ts      # Przykładowy store Zustand (jeśli używany)
│   ├── styles/              # Style globalne
│   │   ├── globals.css      # Globalny CSS (w tym import Tailwinda)
│   │   └── tailwind.config.js # Konfiguracja Tailwind CSS
│   ├── types/               # Globalne definicje typów TypeScript
│   │   ├── api/             # Typy danych z API (np. odpowiedzi REST)
│   │   └── index.d.ts       # (lub wiele plików .d.ts z deklaracjami)
│   └── pages/ or (app/)     # (Opcjonalnie: jeśli używany Pages Router lub mieszana struktura)
└── public/                  # Pliki statyczne (obrazy, favicon itp.)
